# Committed base ssh config. Uniform across machines.
# Machine-specific overrides (e.g. 1Password agent, IdentityAgent none) live in
# the gitignored ~/.ssh/config.local, included first so it wins.
Include ~/.ssh/config.local

Host github.com
	IdentityFile ~/.ssh/id_ed25519
	IdentitiesOnly yes
